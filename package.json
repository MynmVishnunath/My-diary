{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"node --watch server/index.js",
    "start":"node api/index.js",
    "build": "echo 'No build step needed(vishnunath)'"
 
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }

}
