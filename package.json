{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"node --watch server/index.js",
    "start":"node server/index.js"
 
  }
}
